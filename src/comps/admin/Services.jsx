import React from 'react';

const Services = () => {
    return (
        <div>
            lista de servicios (no revisados por defecto, agregar opcion "ver todos") 
        </div>
    );
}

export default Services;
